#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here

prints "Welcome to Tic Tac Toe!"
prints "Choose a number between 1 and 9:"
user_move = gets.strip
index = input_to_index(user_move)
move(board, index)
